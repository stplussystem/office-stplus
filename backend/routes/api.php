<?php

use Illuminate\Http\Request;
use Illuminate\Support\Facades\Route;

// 🟢 โซนสาธารณะ (ใครก็เข้าได้ - เอาไว้ให้หน้าแรกเช็คสถานะ)
Route::get('/test-connection', function () {
    return response()->json([
        'message' => 'สวัสดีคุณ Admin! ระบบออฟฟิสพร้อมทำงานแล้ว 🎉', // เปลี่ยนข้อความให้ดูดีขึ้น
        'status' => 'success',
        'time' => now()->toDateTimeString()
    ]);
});

// 🔒 โซนส่วนตัว (ต้องล็อกอินก่อนถึงเข้าได้)
Route::middleware(['auth:sanctum'])->group(function () {

    // ดึงข้อมูล User คนปัจจุบัน
    Route::get('/user', function (Request $request) {
        return $request->user();
    });

    // เดี๋ยวเราค่อยมาเพิ่ม API อื่นๆ ของระบบออฟฟิสตรงนี้
});
